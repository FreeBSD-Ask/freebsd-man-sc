# pagesize(1)

`pagesize` — 打印系统页面大小

## 名称

`pagesize`

## 概要

`pagesize`

## 描述

`pagesize` 实用程序以字节为单位打印内存页面大小，该值由 getpagesize(3) 返回。此程序在构造可移植的 shell 脚本时很有用。

## 参见

getpagesize(3)

## 历史

`pagesize` 命令出现于 4.2BSD。
