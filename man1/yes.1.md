# yes(1)

`yes` — 不断重复地输出肯定回应

## 名称

`yes`

## 概要

`yes [expletive ...]`

## 描述

`yes` 工具会无限循环地输出 `expletive`，默认输出 "y"。如果给出多个参数，它们会被拼接为一个以空格分隔的单一字符串。

## 参见

jot(1), [seq(1)](seq.1.md)

## 历史

`yes` 命令首次出现于 Version 7 AT&T UNIX。
